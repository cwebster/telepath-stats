<h1>Listing ref_sets</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ref_sets.each do |ref_set| %>
  <tr>
    <td><%= link_to 'Show', ref_set %></td>
    <td><%= link_to 'Edit', edit_ref_set_path(ref_set) %></td>
    <td><%= link_to 'Destroy', ref_set, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ref set', new_ref_set_path %>
