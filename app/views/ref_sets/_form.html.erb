<%= form_for(@ref_set) do |f| %>
  <% if @ref_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ref_set.errors.count, "error") %> prohibited this ref_set from being saved:</h2>

      <ul>
      <% @ref_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
